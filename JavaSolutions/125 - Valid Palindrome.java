class Solution {
    public boolean isPalindrome(String s) {
        String s1 = "";
        String s2 = "";
        s = s.toLowerCase();
        int len = s.length();

        for (int i = 0; i < len; i++) {
        }

        if (s1.equals(s2)) return true;

        return false;
    }
}